gsap.from(".text-overlay h1", {
    duration: 2,
    opacity: 0,
    scale: 1.2,
    ease: "power1.inOut"
});

gsap.from(".text-overlay p", {
    duration: 2,
    opacity: 0,
    y: 30,
    ease: "power1.inOut",
    delay: 0.5
});

    ease: "power1.inOut",
    delay: 0.5
});
